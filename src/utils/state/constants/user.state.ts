export enum UserStatuses {
  PENDING = 'pending',
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
}

export enum UserTransitions {
  ACCOUNT_ACTIVATION = 'account-activation',
  ACCOUNT_SUSPENSION = 'account-suspension',
}
